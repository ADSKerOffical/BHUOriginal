local gui = Instance.new("ScreenGui", game.CoreGui)
gui.ResetOnSpawn = true
gui.DisplayOrder = math.huge

local main = Instance.new("Frame", gui)
main.Draggable = true
main.Active = true
main.Size = UDim2.new(0.6, 0, 0.8, 0)
main.BackgroundColor3 = Color3.new(0, 0, 0)
main.AnchorPoint = Vector2.new(0.5, 0.5)
main.Position = UDim2.new(0.5, 0, 0.5, 0)
main.BorderSizePixel = 4
main.BorderColor3 = Color3.new(1, 1, 1)

local title = Instance.new("TextLabel", main)
title.Text = "UPDATE"
title.Size = UDim2.new(1, 0, 0.2, 0)
title.TextColor3 = Color3.new(1, 1, 1)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundColor3 = Color3.new(0, 0, 0)
title.BorderSizePixel = 4
title.BorderColor3 = Color3.new(1, 1, 1)

local close = Instance.new("TextButton", title)
close.Size = UDim2.new(0.2, 0, 1, 0)
close.Position = UDim2.new(1, 0, 0, 0)
close.TextColor3 = Color3.new(1, 1, 1)
close.BackgroundColor3 = Color3.new(1, 0, 0)
close.BorderSizePixel = 0
close.Text = "X"

close.MouseButton1Click:Connect(function()
   gui:Destroy()
end)

local scroll = Instance.new("ScrollingFrame", main)
scroll.Size = UDim2.new(0.7, 0, 0.4, 0)
scroll.AnchorPoint = Vector2.new(0.5, 0.5)
scroll.Position = UDim2.new(0.5, 0, 0.5, 0)
scroll.BackgroundTransparency = 1

local text = Instance.new("TextLabel", scroll)
text.Text = [[Hello dear user. I'm sorry that the script doesn't work because there was a glitch in the Orion library. I have created the second part of this script with Fluent library, but it still needs tests. As for the function of the second part, I added a lot of things there: 
1) I've added game support. The hub itself determines which playset you are playing on, and if the game is found, it offers you run the script for this game instead of the universal or not at all. Supported Games:

   random tool, doge head escape, survive in area 51, fling things and peoples, infectious smile, bendy and the ink machine, bridge to brainrot, furry infection, build & steal time, cool rarities, +1 speed bike tsunami escape, :) (98874498156203), super power simulator: chakra, push the dummies into sparta hole, cut trees, guns vs fnaf, strongman simulator, saber simulator, duck escape, find the floppa morphs, survive and kill the killers in area 51, scp games and scp monsters, scp monsters or Trevor creatures, lucky block battlegrounds, escape running head, find the chomiks, gang up on people simulator, backrooms race, backrooms motion, cheese escape, cheese escape: the lurking, climb 1000 starts, weapon fighting, become hungry worm, prop morph tower, legends of speed, hide and seek extreme, shrek in the backrooms, backrooms Redacted, 3008. Its all
   
2) Bypassing Adonis anti cheat
3) Icon with my logo of Budgie Hub. You can setup this icon in settings of hub
4) I add search textbox in Main tab and scroll buttons (they are disabled by default)
5) In creator tab i add "Feedback" so you can tell me about bugs or shortcomings of the hub, but you can send one request every 10 minutes

And much more. Execute Budgie Hub Universal 2?
]]

text.Size = UDim2.new(1, 0, 1, 0)
text.TextColor3 = Color3.new(1, 1, 1)
text.TextWrapped = true
text.BackgroundTransparency = 1

local yesB = Instance.new("TextButton", main)
yesB.BackgroundColor3 = Color3.new(0, 0, 0)
yesB.Position = UDim2.new(0.1, 0, 0.85, 0)
yesB.Text = "Execute BHU2"
yesB.BorderSizePixel = 1
yesB.TextSize -= 1
yesB.Size = UDim2.new(0.2, 0, 0.1, 0)
yesB.BorderColor3 = Color3.new(1, 1, 1)
yesB.TextColor3 = Color3.new(1, 1, 1)

yesB.MouseButton1Click:Connect(function()
   gui:Destroy()
   loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/ADSKerOffical/BHU2Project/refs/heads/main/BHU2Script.lua"))()
end)

local noB = Instance.new("TextButton", main)
noB.BackgroundColor3 = Color3.new(0, 0, 0)
noB.Position = UDim2.new(0.7, 0, 0.85, 0)
noB.Text = "Keep BHU"
noB.BorderSizePixel = 1
noB.TextSize -= 1
noB.Size = UDim2.new(0.2, 0, 0.1, 0)
noB.BorderColor3 = Color3.new(1, 1, 1)
noB.TextColor3 = Color3.new(1, 1, 1)

noB.MouseButton1Click:Connect(function()
   gui:Destroy()
   loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/ADSKerOffical/BHUOriginal/refs/heads/main/Source2.luau"))()
end)
